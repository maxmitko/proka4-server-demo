html(lang='ru')
    style(type="text/css").
        table {
            border-collapse: collapse;
        }
        table td {
            border: 1px solid black;
            padding: 0 10px;
        }
    body
        ul
            li= `Заказчик : ${customer}`
            li= `Телефон : ${phone}`
        table
            include includes/order/order-list
